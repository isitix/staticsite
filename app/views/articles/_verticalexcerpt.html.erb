<section>
    <ul class="divided">
        <% articles.first(3).each do |article| %>
            <li>
                <!-- Excerpt -->
                <%= render partial: 'articles/excerpt', locals: {article: article} %>
            </li>
        <% end %>
    </ul>
</section>

